modules = ["python-3.12"]
[agent]
expertMode = true

[nix]
channel = "stable-25_05"
packages = ["postgresql"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Teams API"

[[workflows.workflow]]
name = "Teams API"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd teams && uv run uvicorn main:app --host 0.0.0.0 --port 8080 --reload"
waitForPort = 8080

[workflows.workflow.metadata]
outputType = "console"

[[ports]]
localPort = 8080
externalPort = 8080
