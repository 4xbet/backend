# Структурные паттерны проектирования в микросервисе Teams

## Обзор

В микросервисе Teams реализованы **3 структурных паттерна проектирования**. Все паттерны написаны вручную (не используются библиотечные реализации) и четко идентифицируются в коде через комментарии и архитектуру.

---

## 1. 🏛️ FACADE (Фасад)

### Описание
Паттерн **Facade** предоставляет упрощенный интерфейс к сложной подсистеме работы с командами и атлетами. Он скрывает сложность взаимодействия между несколькими репозиториями и бизнес-логикой.

### Расположение
📁 `teams/src/services/team_service.py`

### Класс
```python
class TeamServiceFacade
```

### Назначение
- Упрощает использование подсистемы для клиентского кода
- Снижает связанность между клиентом и сложной системой
- Централизует бизнес-логику в одном месте

### Основные методы

#### `create_team_with_athletes(team_data, athletes_data)`
Создание команды вместе с несколькими атлетами за одну операцию. Без фасада клиент должен был бы вручную создавать команду, получать её ID, затем по очереди создавать каждого атлета.

#### `get_team_full_info(team_id)`
Получение полной информации о команде, включая всех атлетов и статистику (средний возраст). Фасад собирает данные из разных репозиториев и вычисляет агрегированную информацию.

#### `transfer_athlete(athlete_id, new_team_id)`
Перевод атлета в другую команду. Фасад управляет проверкой существования обоих объектов и корректностью операции.

#### `delete_team_cascade(team_id)`
Удаление команды вместе со всеми её атлетами (каскадное удаление).

#### `get_teams_statistics()`
Получение общей статистики по всем командам (количество команд, атлетов, распределение по странам, самая большая/маленькая команда).

### Пример использования

```python
# Инициализация фасада
team_service = TeamServiceFacade(team_repo, athlete_repo)

# Создание команды с атлетами одной операцией
team = await team_service.create_team_with_athletes(
    team_data=TeamCreate(...),
    athletes_data=[
        AthleteCreate(...),
        AthleteCreate(...),
    ]
)

# Получение полной информации
info = await team_service.get_team_full_info(team_id=1)
# Вернёт: team, athletes, athletes_count, average_age
```

### API Endpoints
- `POST /patterns/facade/team-with-athletes` - Создание команды с атлетами
- `GET /patterns/facade/team/{team_id}/full-info` - Полная информация о команде
- `GET /patterns/facade/statistics` - Статистика по командам

---

## 2. 🎨 DECORATOR (Декоратор)

### Описание
Паттерн **Decorator** динамически добавляет дополнительную функциональность (логирование и мониторинг) к объектам репозиториев без изменения их исходного кода.

### Расположение
📁 `teams/src/db/repositories/logging_decorator.py`

### Класс
```python
class RepositoryLoggingDecorator(BaseRepository[T])
```

### Назначение
- Добавляет функциональность прозрачно для клиента
- Соблюдает принцип открытости/закрытости (Open/Closed Principle)
- Можно комбинировать несколько декораторов
- Не изменяет оригинальный класс репозитория

### Добавляемая функциональность

1. **Логирование всех операций** - каждая операция с репозиторием логируется с деталями
2. **Измерение времени выполнения** - фиксирует время выполнения каждой операции
3. **Счетчик операций** - подсчитывает количество выполненных операций
4. **Статистика** - предоставляет статистику по использованию репозитория
5. **Управление логированием** - можно включать/выключать логирование динамически

### Декорируемые методы
- `get(id)` - получение по ID с логированием
- `list(limit, offset)` - получение списка с логированием
- `create(entity)` - создание с логированием
- `update(id, entity)` - обновление с логированием
- `delete(entity_id)` - удаление с логированием

### Дополнительные методы
- `get_statistics()` - получить статистику операций
- `enable_logging()` - включить логирование
- `disable_logging()` - выключить логирование

### Пример использования

```python
# Создание декорированного репозитория
original_repo = TeamRepository(db)
decorated_repo = RepositoryLoggingDecorator(original_repo, log_operations=True)

# Все операции будут автоматически логироваться
teams = await decorated_repo.list()
# Лог: [DECORATOR] List operation #1: Team (limit=100, offset=0)
# Лог: [DECORATOR] List operation completed: 5 items in 0.123s

# Получение статистики
stats = decorated_repo.get_statistics()
# Вернёт: {"total_operations": 1, "logging_enabled": True, "decorated_model": "Team"}
```

### Использование в приложении

```python
# teams/src/db/repositories/__init__.py
team_repository_with_logging = RepositoryLoggingDecorator(team_repository_instance)
athlete_repository_with_logging = RepositoryLoggingDecorator(athlete_repository_instance)
```

### API Endpoints
- `GET /patterns/decorator/teams-with-logging` - Получить команды с логированием
- `GET /patterns/decorator/team/{team_id}/with-logging` - Получить команду с логированием

---

## 3. 🔌 ADAPTER (Адаптер)

### Описание
Паттерн **Adapter** преобразует интерфейс несовместимого внешнего API в интерфейс, который ожидает наше приложение. Позволяет работать с разными источниками данных через единый интерфейс.

### Расположение
📁 `teams/src/adapters/data_source_adapter.py`

### Классы

#### `IDataSource` (интерфейс)
Определяет общий контракт для всех источников данных.

#### `DatabaseDataSource`
Реализация для работы с локальной PostgreSQL базой данных. Это существующая система (Adaptee).

#### `ExternalAPIDataSource`
Реализация для работы с внешним API. Это несовместимая система с другим форматом данных.

#### `DataSourceAdapter` (адаптер)
Основной адаптер, который унифицирует работу с любым источником данных.

### Назначение
- Позволяет интегрировать несовместимые интерфейсы
- Переиспользование существующего кода
- Соблюдение принципа единственной ответственности (SRP)
- Легко добавлять новые источники данных

### Адаптация форматов

**Внешний API возвращает:**
```json
{
  "teamId": 1,
  "teamName": "Team Name",
  "location": {
    "country": "USA",
    "city": "New York"
  },
  "yearFounded": 2000,
  "logoImage": "http://example.com/logo.png"
}
```

**Наш внутренний формат:**
```json
{
  "id": 1,
  "name": "Team Name",
  "country": "USA",
  "city": "New York",
  "founded_year": 2000,
  "logo_url": "http://example.com/logo.png",
  "source": "external_api"
}
```

### Основные методы

#### `fetch_teams_unified()`
Унифицированное получение команд из любого источника с автоматической адаптацией формата.

#### `fetch_team_by_id_unified(team_id)`
Унифицированное получение команды по ID из любого источника.

#### `merge_data_from_multiple_sources(sources)`
Объединение данных из нескольких источников (БД + API + файлы и т.д.).

### Пример использования

```python
# Работа с локальной БД
db_source = DatabaseDataSource(team_repo)
adapter = DataSourceAdapter(db_source)
teams = await adapter.fetch_teams_unified()

# Работа с внешним API
external_source = ExternalAPIDataSource(api_url="https://api.example.com")
adapter = DataSourceAdapter(external_source)
teams = await adapter.fetch_teams_unified()
# API формат автоматически адаптируется к нашему

# Объединение из нескольких источников
merged = await adapter.merge_data_from_multiple_sources([
    db_source,
    external_source
])
```

### API Endpoints
- `GET /patterns/adapter/teams-from-database` - Данные из БД через адаптер
- `GET /patterns/adapter/teams-from-external-api` - Данные из API через адаптер
- `GET /patterns/adapter/teams-merged` - Объединённые данные из нескольких источников

---

## Дополнительная информация

### Общий Endpoint для просмотра паттернов
`GET /patterns/all-patterns-summary` - Сводная информация по всем реализованным паттернам

### Другие паттерны в проекте

Помимо структурных, в проекте также используются:

**Порождающие паттерны:**
- **Singleton** (`teams/src/db/database.py`) - Database класс является синглтоном

**Поведенческие паттерны:**
- **Chain of Responsibility** (`teams/src/services/error_handlers.py`) - Цепочка обработчиков ошибок
- **Strategy** (`teams/src/services/logger_strategies.py`) - Стратегия логирования

### Структура проекта

```
teams/
├── src/
│   ├── adapters/              # ПАТТЕРН: ADAPTER
│   │   ├── __init__.py
│   │   └── data_source_adapter.py
│   ├── services/              # ПАТТЕРН: FACADE
│   │   ├── team_service.py
│   │   ├── error_handlers.py  # Chain of Responsibility
│   │   ├── logger.py
│   │   └── logger_strategies.py  # Strategy
│   ├── db/
│   │   └── repositories/      # ПАТТЕРН: DECORATOR
│   │       ├── base.py
│   │       ├── team_repository.py
│   │       ├── athlete_repository.py
│   │       └── logging_decorator.py
│   └── api/
│       └── v1/
│           └── patterns_demo.py  # Demo endpoints
└── DESIGN_PATTERNS.md          # Эта документация
```

### Преимущества использования паттернов

1. **Facade** - Упрощает API для клиентов, скрывает сложность
2. **Decorator** - Добавляет функциональность без изменения кода
3. **Adapter** - Позволяет интегрировать несовместимые системы

### Как протестировать

1. Запустите сервер: `uvicorn main:app --reload`
2. Откройте Swagger UI: `http://localhost:8080/docs`
3. Найдите раздел "Design Patterns Demo"
4. Протестируйте каждый endpoint для демонстрации паттернов

---

**Автор:** Реализовано в микросервисе Teams  
**Дата:** 2025  
**Тип паттернов:** Структурные (Structural Design Patterns)
